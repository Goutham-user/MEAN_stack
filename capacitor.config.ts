import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: ' mymessages',
  webDir: 'dist/mymessages',
  bundledWebRuntime: false
};

export default config;
